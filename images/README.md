# 图片目录

目前首页右侧视觉由 CSS 绘制，因此网站无需任何图片也能运行。

以后可把头像或项目图片放在这个文件夹，例如 `images/profile.jpg`。在 `index.html` 中使用相对路径：

```html
<img src="images/profile.jpg" alt="Yorke 的头像">
```

请把 `alt` 改成符合图片内容的简短描述，并尽量压缩图片体积，让手机端加载更快。
